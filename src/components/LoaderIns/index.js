import Loader from 'components/shared/Loader'
import APaper from 'components/shared/APaper'

const LoaderIns = () => (
  <APaper sx={{background: 'black'}}>
    <Loader />
  </APaper>
)

export default LoaderIns
