import { Grid } from "@mui/material";
import { Link } from "react-router-dom";

const ShowCase = () =>{
    return (
        <Grid>
            <Link className='App-link'>To Do List</Link>
        </Grid>
    )
}

export default ShowCase